# SEC EDGAR API Configuration (REQUIRED)
# The app will fail to start if these are not set
APP_NAME=MAFinancingApp
ADMIN_EMAIL=your.email@example.com

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ma_financing

# Redis
REDIS_URL=redis://localhost:6379/0

# Attribution config path (relative to backend/)
ATTRIBUTION_CONFIG_PATH=config/attribution_config.json

# Debug mode
DEBUG=false
